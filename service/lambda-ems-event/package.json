{
  "name": "aws-ems-event",
  "version": "1.0.0",
  "description": "AWS MediaConvert event lambda for Async Video Processing Service",
  "main": "index.js",
  "scripts": {
    "build": "npm ci --only=production && npm cache clean --force && zip -r emsEvent.zip index.js node_modules",
    "test": "jest",
    "lint": "eslint . --ext .js",
    "lint:fix": "eslint . --ext .js --fix",
    "format": "prettier --write ."
  },
  "author": "solo1h",
  "license": "MIT",
  "type": "commonjs",
  "devDependencies": {
    "eslint": "^9.35.0",
    "eslint-config-prettier": "^10.1.8",
    "eslint-plugin-import": "^2.32.0",
    "globals": "^16.4.0",
    "prettier": "^3.6.2"
  },
  "dependencies": {
    "pg": "^8.16.3"
  }
}
